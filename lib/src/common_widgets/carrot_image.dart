import 'package:flutter/cupertino.dart';

class CarrotImage extends StatelessWidget {
  const CarrotImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/photo/splash_screen_logo.png',
      width: 54.7,
      height: 63.61,
    );
  }
}
